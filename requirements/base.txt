# Wheel 0.25+ needed to install certain packages on CPython 3.5+
# like Pillow and psycopg2
# See http://bitly.com/wheel-building-fails-CPython-35
# Verified bug on Python 3.5.1
wheel==0.30.0

django-admin-bootstrapped
# Conservative Django
django==2.0.2 # pyup: < 2.1

# Configuration
django-environ==0.4.4

# Models
django-model-utils==3.1.1

# Password storage
argon2-cffi==18.1.0

# For user registration, either via email or social
# Well-built with regular release cycles!
django-allauth==0.35.0

# Python-PostgreSQL Database Adapter
psycopg2-binary

# Unicode slugification
awesome-slugify==1.6.5

# Time zones support
pytz==2018.3

celery
markdown

# Your custom requirements go here
djangorestframework
django-rest-auth
django-rest-swagger
lxml
django-cors-headers
django-filter

-e git+git@gitlab.com:serial-lab/EParseCIS.git#egg=eparsecis
-e git+git@gitlab.com:serial-lab/EPCPyYes.git#egg=epcpyyes
-e git+git@gitlab.com:serial-lab/quartet_capture.git#egg=quartet_capture
-e git+git@gitlab.com:serial-lab/quartet_epcis.git#egg=quartet_epcis
-e git+git@gitlab.com:serial-lab/serialbox.git#egg=serialbox
-e git+git@gitlab.com:serial-lab/random-flavorpack.git#egg=random_flavorpack
-e git+git@gitlab.com:serial-lab/quartet_manifest.git#egg=quartet_manifest
